{
  "mcpServers": {
    "pdf-layout-translator": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${PLUGIN_ROOT}",
        "--locked",
        "${PLUGIN_ROOT}/server.py"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
